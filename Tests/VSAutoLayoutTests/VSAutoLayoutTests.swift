import XCTest
@testable import VSAutoLayout

final class VSAutoLayoutTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(VSAutoLayout().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
